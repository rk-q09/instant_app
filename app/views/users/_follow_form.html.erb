<% if current_user?(@user) %>
  <p><%= link_to "プロフィール編集", edit_user_registration_path %></p>
<% else %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
